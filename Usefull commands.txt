git pull
git commit -a -m "COMMENT" #To add and commit simultaneously
git push
git checkout <branch name> #To change the branch

git log --graph --oneline --all

az webapp deployment user set --user-name VaishU2235 --password flask123
az group create --name myResourceGroup2 --location "West US"
az appservice plan create --name myAppServicePlan --resource-group myResourceGroup2 --sku FREE
az webapp create --resource-group myResourceGroup --plan myAppServicePlan --name TheTrio --runtime "python|3.6" --deploymentlocal-git
az webapp log tail --resource-group myResourceGroup --name TheTrio